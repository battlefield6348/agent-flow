services:
  agent-flow:
    build: .
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - ./:/app
      - ${HOME}/projects:/workspace
      - ${HOME}/.codex:/root/.codex
      - ${AGY_BIN:-${HOME}/.local/bin/agy}:/usr/local/bin/agy:ro
